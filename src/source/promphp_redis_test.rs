//! Tests for promphp Redis source.

#[test]
fn test_source_creation() {
    // This test requires a running Redis instance
    // In CI, we'd use testcontainers or mock
    // Integration tests are in tests/e2e/
}
